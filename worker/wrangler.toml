name = "hunyuan-photo-proxy"
main = "hunyuan-proxy.js"
compatibility_date = "2024-11-01"

# 部署后通过 secret 注入混元 API Key（不要写在这里、不要进 git）：
#   wrangler secret put HUNYUAN_API_KEY
#
# 本地调试时把 key 放在 worker/.dev.vars（已被 .gitignore 忽略）：
#   HUNYUAN_API_KEY = "你的key"
